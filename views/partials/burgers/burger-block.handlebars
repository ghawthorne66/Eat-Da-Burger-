<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newstatus="{{devoured}}">
		{{#if devoured}}NEW BURGERS!{{else}}THAT WAS GOOD, I APPROVE!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
