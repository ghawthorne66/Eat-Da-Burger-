<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newstatus="{{devoured}}">
		{{#if devoured}}NEW BURGERS!{{else}}YUM!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
